import { ScrollAnimateDirective } from './scroll-animate.directive';

describe('ScrollAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
